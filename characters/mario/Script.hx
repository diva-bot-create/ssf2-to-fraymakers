// Script.hx for mario — converted from SSF2 timeline code
// Review all TODO items before using in production.

function initialize() {
	// Called once on character creation
}

function update() {
	// Called every frame
}

function inputUpdateHook(pressedControls:ControlsObject, heldControls:ControlsObject) {
}

// ── Attack-specific frame scripts ────────────────────────────────────────

// SSF2 source → Fraymakers 'frame1'
// frame1
function frame1_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame10'
// frame10
function frame10_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame11'
// frame11
function frame11_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame12'
// frame12
function frame12_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame13'
// frame13
function frame13_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame14'
// frame14
function frame14_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame15'
// frame15
function frame15_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame16'
// frame16
function frame16_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame17'
// frame17
function frame17_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame18'
// frame18
function frame18_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame19'
// frame19
function frame19_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame2'
// frame2
function frame2_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame20'
// frame20
function frame20_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame21'
// frame21
function frame21_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame22'
// frame22
function frame22_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame23'
// frame23
function frame23_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame24'
// frame24
function frame24_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame25'
// frame25
function frame25_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame26'
// frame26
function frame26_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame27'
// frame27
function frame27_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame28'
// frame28
function frame28_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame29'
// frame29
function frame29_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame3'
// frame3
function frame3_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame30'
// frame30
function frame30_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame31'
// frame31
function frame31_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame32'
// frame32
function frame32_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame33'
// frame33
function frame33_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame34'
// frame34
function frame34_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame35'
// frame35
function frame35_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame36'
// frame36
function frame36_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame37'
// frame37
function frame37_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame38'
// frame38
function frame38_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame39'
// frame39
function frame39_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame4'
// frame4
function frame4_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame40'
// frame40
function frame40_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame41'
// frame41
function frame41_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame42'
// frame42
function frame42_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame43'
// frame43
function frame43_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame44'
// frame44
function frame44_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame45'
// frame45
function frame45_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame46'
// frame46
function frame46_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame47'
// frame47
function frame47_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame48'
// frame48
function frame48_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame49'
// frame49
function frame49_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame5'
// frame5
function frame5_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame50'
// frame50
function frame50_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame51'
// frame51
function frame51_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame52'
// frame52
function frame52_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame53'
// frame53
function frame53_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame54'
// frame54
function frame54_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame55'
// frame55
function frame55_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame56'
// frame56
function frame56_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame57'
// frame57
function frame57_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame58'
// frame58
function frame58_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame59'
// frame59
function frame59_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame6'
// frame6
function frame6_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame60'
// frame60
function frame60_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame61'
// frame61
function frame61_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame62'
// frame62
function frame62_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame63'
// frame63
function frame63_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame64'
// frame64
function frame64_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame65'
// frame65
function frame65_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame66'
// frame66
function frame66_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame67'
// frame67
function frame67_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame68'
// frame68
function frame68_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame69'
// frame69
function frame69_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame7'
// frame7
function frame7_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame70'
// frame70
function frame70_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame71'
// frame71
function frame71_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame72'
// frame72
function frame72_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame73'
// frame73
function frame73_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame74'
// frame74
function frame74_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame75'
// frame75
function frame75_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame76'
// frame76
function frame76_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame77'
// frame77
function frame77_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame78'
// frame78
function frame78_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame79'
// frame79
function frame79_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame8'
// frame8
function frame8_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame80'
// frame80
function frame80_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame81'
// frame81
function frame81_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame82'
// frame82
function frame82_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame83'
// frame83
function frame83_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame84'
// frame84
function frame84_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame85'
// frame85
function frame85_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

// SSF2 source → Fraymakers 'frame9'
// frame9
function frame9_framescript(frame:Int) {
	switch (frame) {
		case 0:
			// SSF2: stop()
		}
	}
}

